[package]
name = "circuit_disclose_settlement"
type = "bin"
authors = ["QuietBook Team"]
compiler_version = "=1.0.0"

[dependencies]
stellar_confidential_lib = { path = "../../.upstream/stellar-contracts/packages/tokens/src/confidential/circuits/lib" }
